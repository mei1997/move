# 控制移动技能（Move）

## 1. 背景

人与电脑的日常交互，大量依赖：

- **看界面**（感知当前屏幕内容）
- **动手操作**（鼠标点击、滚动、键盘输入）

本项目把上述过程自动化：用户用自然语言描述目标，程序像真人一样「观察 → 决策 → 操作」，逐步完成任务。

## 2. 目标

| 项 | 说明 |
|----|------|
| 输入 | 一段自然语言指令（如「打开微信，给文件传输助手发：你好」） |
| 过程 | 按环境选择执行后端，多步完成桌面操作 |
| 输出 | 在真实桌面上完成指令，或明确失败原因 |

核心能力可概括为：**理解用户意图，在真实电脑上可靠地完成界面操作。**

## 3. 实现方式（多路线）

程序**启动时检测运行环境**，选择条件分支；不是只走「截屏 + 鼠标」一条路。

```text
启动
  │
  ├─ 探测 OS / 权限
  │
  ├─ macOS ──► 策略 A：Accessibility First（无障碍树优先）
  │              ├─ 按控件名 click / 设值（不依赖像素）
  │              └─ 失败 ──► 回退策略 B
  │
  ├─ Windows / Linux / 其它 ──► 策略 B：Vision + Mouse（当前通用实现）
  │
  └─ 统一外层：自然语言规划 → 多步循环 → 校验重试
```

### 3.1 策略 A：无系统无障碍树（macOS 推荐主路径）

| 项 | 说明 |
|----|------|
| 技术 | macOS Accessibility / `AXUIElement`；本仓库经 **AppleScript `System Events`** 调用 |
| 做法 | 按名字查找控件（如「文件传输助手」）→ `click` / 聚焦后输入 |
| 优点 | **不依赖像素坐标**，不易点偏到相邻行（如「微信支付」） |
| 缺点 | 部分 Electron / 游戏暴露控件差；需开启「辅助功能」权限 |
| 适合 | 微信、系统设置、多数原生 App 的按钮 / 列表项 |

启动日志示例：

```text
环境: platform=macos system=Darwin/arm64
策略: accessibility_first — 检测到 macOS：优先 Accessibility…
Accessibility 可用: True
[AX] ax_click ok process='WeChat' name='文件传输助手'
```

若 AX 找不到控件，自动回退到策略 B 的像素点击。

### 3.2 策略 B：截屏 + 视觉模型 + 键鼠（通用兜底）

```text
截屏 → 多模态模型决策（含 target_bbox）→ 模拟键鼠 → 校验/重试
```

| 项 | 说明 |
|----|------|
| 优点 | 不依赖控件树，界面「看得见」即可尝试 |
| 缺点 | 坐标可能偏差；控制端窗口可能抢焦点 |
| 增强 | 打开即最大化、bbox 偏上中心点击、动作后校验重试、安静模式 |

### 3.3 其它可选路线（规划中）

| 路线 | 说明 |
|------|------|
| 应用 API / 脚本 | 有官方接口或 Shortcuts / AppleScript 业务入口时最稳 |
| OCR / 模板匹配 | 固定界面、低成本；泛化弱 |
| 录制回放 | 流程固定时可用；分辨率变化易失效 |
| Windows UI Automation / Linux AT-SPI | 对应平台上的「无障碍树」等价物，可对标策略 A |

## 4. 统一控制循环

无论走 AX 还是视觉点击，外层循环一致：

```text
用户指令
    │
    ▼
打开/激活目标应用（可最大化）
    │
    ▼
┌────────────────────────────────────┐
│ 感知：截屏 + 前台应用名              │
│ 技能：按应用加载 skills/apps/*.json │
│ 决策：视觉模型产出【一步】细动作     │
│ 执行：AX 优先 或 像素键鼠            │
│ 校验：未达标则修正重试               │
└───────────────┬────────────────────┘
                ▼
          完成 / 失败 / 下一步
```

### 4.1 应用技能库（按 App 存储使用方式）

每个常见应用维护一张「技能卡」，记录该应用的细粒度用法，供检测到该前台应用时注入决策：

| 内容 | 例子（微信） |
|------|----------------|
| 界面结构 | 三栏：导航 / 会话列表 / 聊天区 |
| 细步骤 | 开会话 → 点输入框 → type → **Return 发送** |
| 硬规则 | Return=发送；Shift+Return=换行 |
| 完成自检 | 输入框仍有全文且气泡未出现 → 未完成，需再按 Return |

存放路径：`skills/apps/<app>.json`（模板见 `_template.json`）。

当前已内置：

| 文件 | 应用 |
|------|------|
| `wechat.json` | 微信（Return 发送等） |
| `feishu.json` | 飞书（发送快捷键可配置、Command+K 搜索） |
| `qq.json` | QQ（发送键可配置） |
| `tonghuashun.json` | 同花顺（F3/F5/F12 等看盘与高风险下单约束） |

新增应用：复制模板，填写 `aliases` / `skills` / `rules`，下次识别到该前台应用会自动加载。

这样可避免「字已打进输入框却不知道要回车发送」这类差一步失败。

## 5. 能力范围

### 5.1 已实现

- 启动时环境探测与策略分支（`src/runtime/platform.py`）
- macOS Accessibility 按名点击 + 失败回退视觉点击（`src/action/accessibility_mac.py`）
- **应用技能库**（`skills/apps/` + `src/skills/store.py`），按前台 App 注入细步骤
- 自然语言多步桌面操控
- 截屏规划、键鼠模拟、打开应用、前台应用识别
- bbox 中心点击、动作后校验重试、安静模式日志

### 5.2 后续可增强

- 运行中把成功路径写回技能卡（自动沉淀）
- AX 路径支持「设值 / 选菜单」等更多动作类型
- Windows UI Automation / Linux AT-SPI 对称实现
- 高风险操作（支付、删文件）确认闸门

## 6. 典型场景

- 打开指定应用并进入某功能页
- 在聊天列表中按**名称**选中联系人并发送消息（macOS 优先 AX）
- 在设置类界面中点选开关、输入内容

## 7. 运行约束

1. **权限**：macOS 需「辅助功能」；走视觉路径还需「屏幕录制」。  
2. **模型**：策略 B 规划侧需支持视觉输入；AX 命中时仍可用模型理解「下一步要点谁」。  
3. **焦点**：控制端（终端 / IDE）可能抢前台；建议安静模式并隐藏控制窗口。  
4. **AX 覆盖**：控件未暴露时会回退像素点击，此时仍可能出现相邻行点偏，依赖校验重试。

## 8. 与工程文档的关系

| 文档 | 用途 |
|------|------|
| `read.md`（本文） | 技能定义、多路线实现与环境分支（产品 / 设计说明） |
| `README.md` | 安装、配置、命令行用法（工程说明） |

---

**一句话**：Move 按环境选操控后端，并按前台应用加载技能卡（细步骤与快捷键约定），用自然语言驱动真实桌面任务。
